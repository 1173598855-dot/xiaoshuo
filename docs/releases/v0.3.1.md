# v0.3.1

## 审查修复

- 修复重新打开作品时丢失 production run 记忆发送配置的问题；现在会从 SQLite 正确恢复“自动推荐 / 仅发送选中”。
- 修复记忆中心在按类型筛选时仍允许勾选已归档记忆的问题。
- 升级 Hono 至 `4.13.7`，并将 `js-yaml` 锁定到 `4.3.2`。

## 验证

```text
221 tests passed
npm run lint
npm run typecheck
npm run build
npm run smoke:desktop
npm run desktop:test
npm run desktop:dist
npm run desktop:package:test
npm run desktop:installed:test
npm audit --omit=dev --audit-level=moderate
```
