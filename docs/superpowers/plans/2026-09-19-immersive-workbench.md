# 沉浸式创作工作台实施计划

用户已批准：统一组件、三栏工作区、立体作品入口及连续交互。

## 验收

- 保留候选隔离、revision 乐观锁与现有 API/IPC 安全边界。
- lint、typecheck、test:run、build、e2e 通过。
- 1440×960、1024×768、390×844 检查真实操作路径、裁切、横向溢出。
- 键盘可进入作品、调整工作区并退出专注模式；reduced-motion 保留全部功能。
- 实际记录交互性能与尚未验证的限制。

## 实施

- [x] 建立可追踪目标，读取当前主流程规格、README 和相关测试。
- [x] 作品书架组件：CSS 三维封面、书脊、书页厚度，真实作品入口及紧凑列表切换。
- [x] 书架交互测试先失败后通过；首页原有草稿/预设测试通过。
- [x] 合并重复主题块，保留末端权威主题层；将夜间画布、紫色主色、面板和状态色收敛到 tokens.css。
- [x] 章节树—正文/候选—上下文三栏工作区；侧栏宽度调整、收起和专注模式。
- [x] 作品进入增加可跳过的 220ms 立体过渡；方向选择、候选审核、采纳和弹窗继续复用原有业务状态与回调。
- [x] 表单、临时面板、加载、空状态和错误状态统一使用末端主题 token 与同一控件尺寸/焦点规范。
- [x] 首页响应式与 reduced-motion 路径；补充书架、工作区测试，e2e 覆盖工作区键盘路径。
- [x] 生产室三栏完整响应式路径已加入 E2E；首页 1440×960 / 1024×768 / 390×844 的实际横向溢出检查通过。
- [x] README 更新；本地 Chromium 无缓存测得首页 loadMs：桌面 1060、平板 665、移动 613，三种尺寸 scrollWidth 均等于 viewportWidth；该数据不代表 GPU/真实设备 3D 帧率，后续仍需真机或带 GPU 的 Electron 观察。
- [x] 全量质量门禁与最终差异自审；生产室响应式路径由 E2E 实际执行验证，首页保留桌面/移动截图证据。

## 当前验证

BookShelf 新测试初次运行因组件尚不存在而失败；实现后 BookShelf 与 CreativeHome 共 5 项测试通过。WorkspaceLayout 测试 1 项通过，当前单元测试 90 个文件 / 377 项通过，e2e 5 项通过，build 与 smoke:server 通过。E2E 第一条主流程现在会在生产室切换 1440×960、1024×768、390×844，验证三栏工作区、专注模式、Escape 恢复和无横向溢出。性能数据来自本地 headless Chromium，不能代替 GPU 真机测量。
